# Инструкции за стартиране на сървъра след добавяне на i18n поддръжка

## 📋 Стъпки за активиране на интернационализацията

### 1. Пулване на последните промени от Git

```bash
cd /path/to/mybibliotheca
git pull origin main
```

### 2. Активиране на виртуалната среда

**Ако използвате venv:**
```bash
source venv/bin/activate
```

**Ако използвате Docker:**
```bash
# Продължете с Docker командите по-долу
```

### 3. Инсталиране на новите зависимости

```bash
pip install -r requirements.txt
```

Това ще инсталира `Flask-Babel>=4.0.0` и неговите зависимости (`Babel`, `pytz`).

### 4. Компилиране на преводните файлове

```bash
pybabel compile -d translations
```

**Очакван изход:**
```
compiling catalog translations/bg/LC_MESSAGES/messages.po to translations/bg/LC_MESSAGES/messages.mo
compiling catalog translations/en/LC_MESSAGES/messages.po to translations/en/LC_MESSAGES/messages.mo
```

### 5. Рестартиране на приложението

**За development сървър:**
```bash
python dev_run.py
```

**За production с Gunicorn:**
```bash
# Спрете текущия процес
pkill -f gunicorn

# Стартирайте отново
gunicorn -c gunicorn_config.py "app:create_app()"
```

**За Docker:**
```bash
docker-compose down
docker-compose up -d --build
```

### 6. Проверка на инсталацията

1. Отворете приложението в браузър
2. Проверете дали в навигационната лента има dropdown за смяна на езика (икона за превод)
3. Кликнете на dropdown-а и изберете "Български"
4. Проверете дали интерфейсът се променя на български

## 🔍 Проверка за проблеми

### Ако преводите не се показват:

1. **Проверете дали .mo файловете съществуват:**
   ```bash
   ls -la translations/bg/LC_MESSAGES/*.mo
   ls -la translations/en/LC_MESSAGES/*.mo
   ```

2. **Прекомпилирайте преводите:**
   ```bash
   pybabel compile -d translations
   ```

3. **Проверете логовете за грешки:**
   ```bash
   # За development
   tail -f logs/app.log
   
   # За Docker
   docker-compose logs -f app
   ```

### Ако Flask-Babel не е инсталиран:

```bash
pip install Flask-Babel
pip freeze | grep -i babel >> requirements.txt
```

## 📝 Важни бележки

1. **Преводите са готови за използване**, но за да се използват в кода, трябва да маркирате низовете с `_()` функцията:
   - В Python: `from flask_babel import gettext as _`
   - В templates: `{{ _('Text to translate') }}`

2. **По подразбиране езикът е English**. Потребителите могат да сменят езика чрез dropdown менюто в навигацията.

3. **Езикът се запазва в session**, така че изборът на потребителя се запазва между заявките.

4. **За да добавите нови преводи:**
   ```bash
   # 1. Извлечете новите низове
   pybabel extract -F babel.cfg -o messages.pot .
   
   # 2. Обновете съществуващите .po файлове
   pybabel update -i messages.pot -d translations
   
   # 3. Редактирайте translations/bg/LC_MESSAGES/messages.po и добавете преводи
   
   # 4. Компилирайте отново
   pybabel compile -d translations
   
   # 5. Рестартирайте приложението
   ```

## ✅ Готово!

След като изпълните всички стъпки, приложението трябва да работи с поддръжка за два езика: English и Български.

За допълнителна информация вижте: `I18N_IMPLEMENTATION_GUIDE.md`
